## Problem


## Example
```python
Input: nums = [1,2,3,2,2]

Output: 2
```

## Solution 1
```python
class Solution:
    def findDuplicate(self, nums: List[int]) -> int:
        counts = {}

        for num in nums:
            counts[num] = 1 + counts.get(num, 0)
            if counts[num] > 1:
                return num
```

Explain: 
## Problem
Given an array of integers `nums`, return _the length_ of the longest consecutive sequence of elements that can be formed.

A _consecutive sequence_ is a sequence of elements in which each element is exactly `1` greater than the previous element. The elements do _not_ have to be consecutive in the original array.

You must write an algorithm that runs in `O(n)` time.

## Example
```python
Input: nums = [2,20,4,10,3,4,5]

Output: 4
```

## Solution
```python
class Solution:
    def longestConsecutive(self, nums: List[int]) -> int:
        nums = list((sorted(list(set(nums)))))
        
        ans = 0
        prev = 0

        if len(nums) == 0: return 0
        if len(nums) == 1: return 1

        for i in range(len(nums) - 1):
            if i == 0: ans += 1
            if nums[i + 1] - nums[i] == 1:
                ans += 1
            else:
                if ans > prev:
                    prev = ans
                ans = 1

        return prev if prev > ans else ans

```

First, we can create a HashMap to store the longest consecutive sequence at any current number.

If that number is already in the HashMap, then we can skip it. If that number - 1 is in the HashMap, then we know that we already hAV